# Copy to .env.e2e and fill in. .env.e2e is gitignored; this file is not.
#
# The suite writes real rows (it creates and then deletes a job), so point it
# at an account whose data you are willing to have touched.
E2E_BASE_URL=
E2E_ADMIN_EMAIL=
E2E_ADMIN_PASSWORD=
